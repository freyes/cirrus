accounts:
  aws:
    access_key: "ACCESS_KEY"
    secret_key: "SECRET_KEY"
    type: ec2
  trystack:
    access_key: ACCESS_KEY
    secret_key: SECRET_KEY
    endpoint: https://nova-api.trystack.org:5443/v2.0
    type: openstack
